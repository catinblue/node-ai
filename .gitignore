# ═══ SECRETS ═══
.env

# ═══ GENERATED OUTPUT ═══
# digest.html is tracked — it IS the deployed app (public news data, not private)

# ═══ DATABASE (CI persists via actions/cache, not git) ═══
news.db
news.db-wal
news.db-shm

# ═══ SESSION ARTIFACTS (private development history) ═══
docs/

# ═══ SCREENSHOTS (Playwright test output) ═══
*.png
*.jpg
*.jpeg

# ═══ TOOL CONFIGS (local only) ═══
.claude/
.superpowers/
.playwright-mcp/

# ═══ PYTHON ═══
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
.venv/
venv/

# ═══ OS ═══
.DS_Store
Thumbs.db
desktop.ini
ehthumbs.db

# ═══ IDE ═══
.vscode/
.idea/
*.swp
*.swo
*~

# ═══ MISC ═══
*.log
*.tmp
node_modules/
package.json
package-lock.json
images/

.vercel
